import { HTTPAlias } from 'got';
declare type RequestOptions = {
    method: HTTPAlias;
    path: string;
    queryParams?: object;
    bodyParams?: object;
};
export default class RestClient {
    apiKey: string;
    serverApiUrl: string;
    apiVersion: string;
    httpOptions?: object;
    constructor(apiKey: string, serverApiUrl: string, apiVersion: string, httpOptions?: object);
    makeRequest(requestOptions: RequestOptions): Promise<any>;
}
export {};
